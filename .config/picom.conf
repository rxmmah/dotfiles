backend = "glx";   # Enable OpenGL backend
vsync = true;      # Enable vertical synchronization (vsync)

# Optional: for Intel graphics to force full composition pipeline 
# (might improve performance and reduce tearing)
glx-no-stencil = true; 
glx-no-rebind-pixmap = true;
